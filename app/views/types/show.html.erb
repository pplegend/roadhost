<p id="notice"><%= notice %></p>
<div class="row  types_category">
  <div class="span2 "><ul class="nav"><%= render "layouts/drop_down_menu" %></ul></div>
  <div class="span6 offset2">
  <% if products=@type.products %>
	<% products.each do |product| %>
		<%= link_to product.name, type_product_path(@type,product), :class=>'popover-with-html', "data-original-title"=>"#{product.name}","data-content"=>"#{image_tag product.image.url(:thumb)} <div style='float:left'>#{product.description.html_safe} </div>" %> <br/>
	<% end %>
  <% end %>
  </div>
</div>
<%= link_to 'Edit', edit_type_path(@type) %> |
<%= link_to 'Back', types_path %>


