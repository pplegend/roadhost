<h1>Listing products</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Detail</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= product.name %></td>
    <td><%= product.description %></td>
    <td><%= product.detail_id %></td>
    <td><%= link_to 'Show', type_product_path(@type,product) %></td>
    <td><%= link_to 'Edit', edit_type_product_path(@type,product) %></td>
    <td><%= link_to 'Destroy', type_product_path(@type,product), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product', new_type_product_path(@type) %>
